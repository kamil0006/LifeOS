/** Zapis w `Expense.category` / wierszu transakcji przy braku kategorii */
export const EXPENSE_CATEGORY_NONE = ''

/** Label used in tables and UI (not to be confused with the stored value). */
export const EXPENSE_CATEGORY_DISPLAY_NONE = '-'

/** Category id only from the default set (no API row) — cannot be deleted from the UI. */
export const BUILTIN_EXPENSE_CATEGORY_ID_PREFIX = 'static:'
